node_modules
.next
out
.env*.local*
.DS_Store
*.tsbuildinfo
next-env.d.ts
.screens
.remember
coverage
.vercel

# Raw camera captures — commit only compressed derivatives in public/video
*.mov

# Raw session dumps (fotos/videos sin optimizar) — commit only optimized
# derivatives in public/photos + public/video
public/recordings/
public/dj-course/
.playwright-mcp/

# Descargas de logs de GitHub Actions ("<paso>_<job>.txt") — se bajan para
# depurar un run y no son fuente; el repo es público, no queremos 100+ KB de
# logs de runner en la historia.
[0-9]_*.txt
